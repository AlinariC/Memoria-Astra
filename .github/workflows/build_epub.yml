name: Build EPUBs

on:
  push:
    paths:
      - "**/Manuscript.md"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-epubs:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y pandoc zip unzip imagemagick

    - name: Build and Polish EPUBs
      run: |
        mkdir -p output
        echo "### Books Published" > release_notes.md
        echo "" >> release_notes.md
        for dir in */; do
          if [ -f "${dir}Manuscript.md" ]; then
            echo "📖 Processing $dir..."
            cd "$dir"
            bash ../scripts/build.sh
            bash ../scripts/fix-and-polish-epub.sh
            SAFE_BOOK=$(ls *.epub | sed 's/.epub$//; s/-/ /g')
            echo "- ${SAFE_BOOK}" >> ../release_notes.md
            mv *.epub ../output/
            cd ..
          fi
        done

    - name: Install GitHub CLI
      run: |
        sudo apt-get install -y gh

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: "epub-${{ github.run_id }}"
        release_name: "EPUB Build - ${{ github.run_number }}"
        body_path: release_notes.md
        draft: false
        prerelease: false

    - name: Upload EPUB Files to Release
      run: |
        TAG_NAME="epub-${{ github.run_id }}"
        for file in output/*.epub; do
          echo "Uploading $file..."
          gh release upload "$TAG_NAME" "$file" --clobber
        done
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
